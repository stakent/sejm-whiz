# Example environment variables for docker-compose
# Copy this file to .env and modify values as needed

# Database configuration
POSTGRES_PASSWORD=your_secure_password_here

# ========================================
# Development GPU/CPU Configuration
# ========================================
# Choose processing mode for development: 'cuda' for GPU (default), 'cpu' for CPU-only
DEV_EMBEDDING_DEVICE=cuda

# GPU Runtime Configuration (leave empty for CPU-only mode)
# For GPU support, uncomment the following lines:
DEV_GPU_RUNTIME=nvidia
DEV_GPU_DRIVER=nvidia
DEV_GPU_COUNT=1

# For CPU-only development (lightweight, faster startup), use:
# DEV_EMBEDDING_DEVICE=cpu
# DEV_GPU_RUNTIME=
# DEV_GPU_DRIVER=
# DEV_GPU_COUNT=0
# PROCESSOR_DOCKERFILE=projects/data_processor/Dockerfile.cpu

# ========================================
# Production GPU Configuration
# ========================================
# GPU configuration (for production)
EMBEDDING_DEVICE=cuda  # or 'cpu' for CPU-only mode
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility
CUDA_VISIBLE_DEVICES=0

# Optional: Custom model cache directory
# MODEL_CACHE_DIR=/path/to/model/cache
