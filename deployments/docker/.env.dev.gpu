# Development environment with GPU support (default)
# Copy this to .env for GPU-enabled development

# Database configuration
POSTGRES_PASSWORD=sejm_whiz_dev_password

# Development GPU Configuration
DEV_EMBEDDING_DEVICE=cuda
PROCESSOR_DOCKERFILE=projects/data_processor/Dockerfile
DEV_GPU_RUNTIME=nvidia
DEV_GPU_DRIVER=nvidia
DEV_GPU_COUNT=1

# NVIDIA GPU settings
NVIDIA_VISIBLE_DEVICES=all
NVIDIA_DRIVER_CAPABILITIES=compute,utility
CUDA_VISIBLE_DEVICES=0
