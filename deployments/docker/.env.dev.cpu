# Development environment with CPU-only processing
# Copy this to .env for lightweight CPU-only development

# Database configuration
POSTGRES_PASSWORD=sejm_whiz_dev_password

# Development CPU Configuration
DEV_EMBEDDING_DEVICE=cpu
PROCESSOR_DOCKERFILE=projects/data_processor/Dockerfile.cpu

# GPU settings (disabled for CPU mode)
DEV_GPU_RUNTIME=
DEV_GPU_DRIVER=
DEV_GPU_COUNT=0
